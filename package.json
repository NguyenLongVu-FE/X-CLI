{
  "name": "@nguyenlongvu-fe/x-cli",
  "version": "0.2.0",
  "description": "Safe browser-backed command-line control of an authenticated X account",
  "license": "MIT",
  "packageManager": "pnpm@11.22.0",
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "x": "dist/cli.js"
  },
  "files": [
    "dist",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "build": "rimraf dist && tsc && chmod +x dist/cli.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:dist": "pnpm build && tsx scripts/dist-smoke.ts",
    "test:git-install": "tsx scripts/git-install-smoke.ts",
    "test:browser-process": "tsx scripts/browser-process-smoke.ts",
    "test:live:browser": "tsx scripts/live-browser-contract.ts",
    "test:skill-install": "pnpm build && tsx scripts/skill-install-smoke.ts",
    "test:audit": "pnpm audit --prod",
    "install:mac": "pnpm test:audit && pnpm build && tsx scripts/install-macos.ts"
  },
  "dependencies": {
    "playwriter": "0.4.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "rimraf": "^6.1.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
